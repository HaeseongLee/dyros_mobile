clc
clear all

addpath('/home/dyros/catkin_ws/src/powered_caster_vehicle')

q = load('pcv_q.txt');
N = length(q);
q_steer = zeros(N, 4);
q_wheel = zeros(N, 4);

for i = 1:4
    q_steer(:, 2*i) = q(:,2*i);
    q_wheel(:, 2*i+1) = q(:,2*i+1);
end

figure(1)
plot(q_steer(:,1));
